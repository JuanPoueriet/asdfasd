# price-lists-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build price-lists-domain` to build the library.

## Running unit tests

Run `nx test price-lists-domain` to execute the unit tests via [Jest](https://jestjs.io).
