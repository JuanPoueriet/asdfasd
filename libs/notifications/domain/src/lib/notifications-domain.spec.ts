import { notificationsDomain } from './notifications-domain';

describe('notificationsDomain', () => {
  it('should work', () => {
    expect(notificationsDomain()).toEqual('notifications-domain');
  });
});
